import { ApiRequestParams } from './interfaces/apiRequest.interface';

export const DEFAULT_REQUEST_PARAMS: ApiRequestParams = {
  pageNumber: 1,
  pageSize: 10,
};

export const API_PATH = 'stories';
